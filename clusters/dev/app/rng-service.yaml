apiVersion: v1
kind: Service
metadata:
  labels:
    app: rng
  name: rng
  namespace: pretest
spec:
  ports:
  - name: 8080
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: rng
  type: ClusterIP
status:
  loadBalancer: {}
