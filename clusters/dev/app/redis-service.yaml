apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis
  name: redis
  namespace: pretest
spec:
  ports:
  - name: 8080
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: redis
  type: ClusterIP
status:
  loadBalancer: {}
