apiVersion: v1
kind: Service
metadata:
  labels:
    app: worker
  name: worker
  namespace: pretest
spec:
  ports:
  - name: worker
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: worker
  type: ClusterIP
status:
  loadBalancer: {}
