apiVersion: v1
kind: Service
metadata:
  labels:
    app: webui
  name: webui
  namespace: pretest
spec:
  ports:
  - name: webui
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: webui
  type: ClusterIP
status:
  loadBalancer: {}
